## plugin-rescope

Claude Code has a bug where installing a plugin at the local or user scope prevents you from reinstalling it into a different project. This package provides workarounds to rescope plugins across projects.
